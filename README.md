# دریافت مرکزی (Daryaft Core)

## توضیحات پروژه

**دریافت مرکزی** یک سیستم مدیریت دریافت‌ها است که با استفاده از فریمورک Spring Boot 3.2.x و Java 21 ساخته شده است.

## مشخصات فنی

- **نسخه Spring Boot**: 3.2.5
- **نسخه Java**: 23
- **ابزار ساخت**: Maven
- **پایگاه داده**: H2 Database (File-based)
- **پورت سرور**: 8081

## وابستگی‌های اصلی

- Spring Web
- Spring Data JPA
- H2 Database
- Spring Boot DevTools
- Lombok
- Spring Validation

## ساختار پروژه

```
src/main/java/com/daryaftmanazam/daryaftcore/
├── model/          # مدل‌های دامنه
├── repository/     # لایه دسترسی به داده
├── service/        # لایه منطق کسب‌وکار
├── controller/     # کنترلرهای REST
├── dto/            # اشیاء انتقال داده
├── config/         # پیکربندی‌های برنامه
├── exception/      # استثناهای سفارشی
└── util/           # کلاس‌های کمکی
```

## نحوه اجرا

### پیش‌نیازها

- Java 21 یا بالاتر
- Maven 3.8 یا بالاتر

### دستورات اجرا

#### کامپایل و اجرای برنامه:
```bash
mvn spring-boot:run
```

#### ساخت فایل JAR:
```bash
mvn clean package
```

#### اجرای فایل JAR:
```bash
java -jar target/daryaft-core-0.0.1-SNAPSHOT.jar
```

## دسترسی به سرویس‌ها

### کنسول H2 Database
- **آدرس**: http://localhost:8081/h2-console
- **JDBC URL**: jdbc:h2:file:./data/daryaftdb
- **نام کاربری**: sa
- **رمز عبور**: (خالی)

### Health Check Endpoint
- **آدرس**: http://localhost:8081/api/health
- **متد**: GET
- **پاسخ نمونه**:
```json
{
  "status": "UP",
  "message": "سیستم دریافت مرکزی به درستی کار می‌کند",
  "timestamp": "2025-12-22T10:30:00",
  "version": "1.0.0"
}
```

## پشتیبانی از زبان فارسی

این پروژه به طور پیش‌فرض از زبان فارسی پشتیبانی می‌کند و تمامی پیام‌ها و پاسخ‌های سیستم به زبان فارسی نمایش داده می‌شوند.

## تنظیمات پایگاه داده

پایگاه داده H2 به صورت فایلی در مسیر `./data/daryaftdb` ذخیره می‌شود و پس از هر بار راه‌اندازی مجدد، داده‌ها حفظ خواهند شد.

### 📊 داده‌های نمونه (Sample Data)

این پروژه شامل دو روش مقداردهی اولیه داده‌های نمونه است:

1. **DataInitializer Component** (روش پیشنهادی - فعال به صورت پیش‌فرض)
   - مقداردهی خودکار هنگام اجرای اولیه
   - شامل 2 کسب‌وکار، 10 مشتری، و داده‌های کامل

2. **data.sql Script** (روش جایگزین)
   - اجرای دستی اسکریپت SQL
   - داده‌های ثابت و قابل پیش‌بینی

برای اطلاعات کامل و نحوه استفاده، به فایل [DATA_INITIALIZATION.md](DATA_INITIALIZATION.md) مراجعه کنید.

**نکته**: پس از اجرای اولیه برنامه، می‌توانید داده‌های نمونه را در کنسول H2 مشاهده کنید.

## توسعه

برای توسعه این پروژه، می‌توانید از Spring Boot DevTools استفاده کنید که قابلیت hot reload را فراهم می‌آورد.

## مجوز

این پروژه تحت مجوز سازمانی منتشر شده است.

## تماس با ما

برای اطلاعات بیشتر، لطفاً با بخش فنی تماس بگیرید.

---

**تاریخ ایجاد**: ۱ دی ۱۴۰۴  
**نسخه**: 0.0.1-SNAPSHOT
